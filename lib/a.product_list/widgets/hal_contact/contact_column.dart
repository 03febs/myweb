import 'package:flutter/material.dart';

class CobaKotak extends StatelessWidget {
  const CobaKotak({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 200,
      width: 200,
      color: Colors.grey,
    );
  }
}
