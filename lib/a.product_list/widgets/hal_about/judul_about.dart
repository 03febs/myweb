import 'package:flutter/material.dart';

class JudulAbout extends StatelessWidget {
  const JudulAbout({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 350,
      height: 100,
      color: Colors.grey,
    );
  }
}
